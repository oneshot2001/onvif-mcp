# BACKLOG — nightly Astra loop input (Claude writes, Matthew edits)

Rules: loop takes the TOP Ready item, up to 3 per night (2026-09-22), one commit each, stops at first BLOCKED. No `accept:` line → loop does not fire.
Branch `night/YYYY-MM-DD` off `main`. Never push. No live camera calls (192.168.1.x) — tests run offline.

## Ready
- [ ] Security C5: if `cred get` fails the password is `""`, the server starts anyway, and every call is a bad-auth attempt that can trip the camera's brute-force lockout. Add `creds-check.ts` exporting a pure `missingPasswords(passwords: Record<string, string>): string[]` (camera ids whose password is empty/whitespace); `index.ts` calls it right after the cred loop and exits non-zero naming those cameras and their `credKey`s (no password text in the message). Add `creds-check.test.ts`: all present → `[]`; one empty and one whitespace-only → both ids — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Move `parseParams` out of `index.ts` into a new `vapix-params.ts` (exported; `index.ts` imports it — same pattern as `snapshot-content.ts`, no behavior change) and add `vapix-params.test.ts`: `root.`-prefixed and unprefixed lines give the same keys, CRLF input, a value containing `=`, non-param/error lines skipped, keys returned sorted — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Move `allowed` and `configDenied` out of `index.ts` into a new `policy-check.ts` as pure functions that take the policy object and agent id as arguments (`index.ts` keeps thin wrappers passing `policy`/`AGENT`; deny strings unchanged) and add `policy-check.test.ts`: unknown agent fails closed, tool not allowlisted, camera not allowlisted, no `config` grant, `config_remediate` with `approve` but no `remediate` grant, and the all-allowed case returns null — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Move the WebSocket message parsing inside `observeEvents` (`index.ts`) into a pure `parseEventMessage(data)` in a new `event-parse.ts` returning `{ event?, warning? }` (`observeEvents` calls it; behavior unchanged) and add `event-parse.test.ts`: an `events:notify` message with a string topic yields the event, an `error` message yields the `event stream error:` warning, invalid JSON yields the invalid-JSON warning, a non-notify message yields neither — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] `lastReceipt` in `index.ts` throws on a `receipts/chain.jsonl` that exists but is empty (`JSON.parse("")`). Move the parsing into a pure `lastReceiptFrom(text: string)` in a new `receipt-chain.ts` that returns `{ seq: 0, hash: "genesis" }` for empty/whitespace text (`lastReceipt` calls it) and add `receipt-chain.test.ts` covering empty text, one line, and many lines — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Add `verifyChain(lines: string[], publicKeyPem: string)` to `receipt-chain.ts` returning `{ count, bad: [{ seq, reason }] }`: for each receipt recompute sha256 over the JSON body with `hash` and `sig` removed (key order as parsed — matches `receipt()` in `index.ts`), check the ed25519 signature over the hash, and check `prev` links to the previous line's hash (`genesis` first). Test with an ed25519 keypair generated inside the test and a 3-receipt chain built in memory: intact chain has no bad entries; a flipped byte, a deleted middle line, and two swapped lines are each reported — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Add `tool-registry.test.ts`, a static drift guard that reads `index.ts` and `commission.ts` as text (do not import `index.ts` — it starts the server): every tool name registered with `server.tool("…"` has a `NODE_KIND` entry in `index.ts` and appears in `policy.json` under `agents.claude-main.tools`; fix any drift the test surfaces — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Extend `render-handoff.test.ts`: copy an existing `handoff/*.json` fixture to a tmp dir, set the spec name and one plan row's `desired` value to a string containing `<script>` and `&`, render without `--pdf`, and assert the HTML contains the escaped forms and no raw `<script>`; fix `render-handoff.ts` only if the test fails — accept: `bun test` exits 0 with the new test; no `--pdf` path exercised; no files written under `handoff/`.
- [ ] Security B2 (needs `verifyChain` in `receipt-chain.ts`): deleting the last N lines of `chain.jsonl` passes `--verify`, and `seq` is never checked. Extend `verifyChain` to report `{ seq, reason: "seq gap" }` when a receipt's `seq` is not the previous `seq + 1` (first receipt must be `seq` 1). Add a pure `missingHandoffHeads(chainHashes: Set<string>, handoffs: Array<{ file: string; chain_head_hash: string }>): string[]` returning files whose recorded head hash is absent from the chain. Make `--verify` in `index.ts` use `verifyChain` and also fail on any missing handoff head, printing `handoff head missing — chain truncated?` with the file names. Tests (in-memory chain, keypair generated in the test): intact chain clean; last line dropped → the handoff that recorded it is reported; a renumbered line → `seq gap` — accept: `bun test` exits 0; `bun index.ts --verify` still reports chain OK on the committed chain (264 receipts, 58 handoffs checked clean 2026-09-20); tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security B1 (needs `receipt-chain.ts`): every MCP client spawns its own server process and all of them read-tail-then-append one `chain.jsonl` with no lock, so two agents writing at once fork the chain (duplicate `seq`, stale `prev`). Add `appendLocked(logPath: string, build: (prev: { seq: number; hash: string }) => string): void` to `receipt-chain.ts`: take an exclusive lock with `openSync(logPath + ".lock", "wx")` (retry on `EEXIST`; if the lockfile's pid is no longer alive, remove it and retry), read the tail via `lastReceiptFrom`, append the built line, release. `receipt()` in `index.ts` builds its line inside the callback. Add `receipt-chain-lock.test.ts`: in a tmp dir, spawn 4 `bun` child processes that each append 25 receipts through `appendLocked` with a test keypair; the resulting 100-line chain passes `verifyChain` with no bad entries and no `seq gap` — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security A5 (needs `receipt-chain.ts`): `get_receipts {n: 0}` returns the entire chain (`slice(-0)`), and any agent granted the tool reads every other agent's params. Add a pure `selectReceipts(lines: string[], agent: string, n: number): string[]` returning the last `n` lines whose parsed `principal.id === agent`; change the tool schema to `z.number().int().min(1).max(100).default(5)`. Tests: `n` of 1 and 5; a chain mixing two agents returns only the caller's; a malformed line is skipped, not thrown — accept: `bun test` exits 0 with the new test; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security A4 (needs `policy-check.ts`): add `policy-matrix.test.ts` — load the real `policy.json` and `cameras.json`, and for every agent × every tool registered in `index.ts`/`commission.ts` (read the files as text, same approach as `tool-registry.test.ts`) × every camera id plus one unknown camera id plus one unknown agent, assert `allowed(...)` is null exactly when the tool is in that agent's `tools` and the camera in its `cameras`, and a deny string otherwise. Explicitly assert `untrusted-demo` gets a deny for every tool except `get_snapshot` on `p3285` — accept: `bun test` exits 0 with the new test file; no production code changed unless the matrix surfaces a real mismatch; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security D2 (needs `policy-check.ts`): `ptz_move` bounds pan/tilt by `ptz.maxStep` but zoom takes any finite number. Add a pure `ptzBound(ptz: { maxStep: number; maxZoomStep?: number } | undefined, pan: number, tilt: number, zoom: number): string | null` to `policy-check.ts` (deny strings for pan/tilt unchanged; zoom denied with `zoom step exceeds policy maxZoomStep <n>`; a missing `maxZoomStep` means only `zoom === 0` is allowed). `ptz_move` in `index.ts` uses it. Add `"maxZoomStep": 25` under `agents.claude-main.ptz` in `policy.json` (Matthew's delegated value, 2026-09-20 — edit freely). Tests: in-bounds passes; pan over, tilt over, zoom over each denied; no `maxZoomStep` + zoom 1 denied, zoom 0 allowed — accept: `bun test` exits 0; the `policy.json` diff is exactly that one key; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security D3: `maxStep` caps one move, not a minute of them — 12 × 30° is a full rotation, and a `get_snapshot` loop fills `receipts/` with JPEGs. Add `rate-limit.ts` exporting `makeLimiter(now: () => number)` with `check(key: string, perMinute: number): string | null` (in-memory sliding 60 s window; returns `rate limit: <n>/min for <key>` when exceeded, records the call otherwise). One server process serves one agent, so in-process state is per-agent. `index.ts` reads optional `rate: { actuationPerMin, snapshotPerMin }` from the agent's policy entry (absent → no limit, current behavior) and checks `actuation` before `ptz_move`, `ptz_preset`, approved `config_remediate`, and approved `commission_apply`, and `snapshot` before `get_snapshot`; a hit is an ordinary receipted `deny`. Add `"rate": { "actuationPerMin": 6, "snapshotPerMin": 30 }` to both agents in `policy.json` (`untrusted-demo` needs only `snapshotPerMin`; Matthew's delegated values, 2026-09-20 — edit freely). No snapshot auto-deletion — frames are evidence. Tests with an injected clock: 6 allowed then the 7th denied; allowed again after 60 s; keys are independent — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security B4: `camera`, `preset`, `param`, and `spec` tool arguments are unbounded `z.string()`, and a denied call still writes the whole string into a signed receipt. Add `schemas.ts` exporting `cameraId = z.string().min(1).max(64)`, `presetName = z.string().min(1).max(128)`, `paramName = z.string().min(1).max(128)`, `specRef = z.string().min(1).max(128)`; use them in every `server.tool(...)` schema in `index.ts` and `commission.ts` (defaults such as `preset` → `"Home"` preserved). Add `schemas.test.ts`: boundary lengths accepted, one over rejected, empty rejected — accept: `bun test` exits 0 with the new test file; `grep -n 'camera: z.string()' index.ts commission.ts` prints nothing; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security D6: `COMMISSION_FAIL_PARAM` (a test hook that forces verify failure and rollback) is honored in production. In `commission.ts` honor it only when `process.env.NODE_ENV === "test"` (`bun test` sets this). Add a test that sets `NODE_ENV` to `production` for one `commission_apply` run with the hook set and asserts no forced failure and no rollback, then restores it — accept: `bun test` exits 0, including the existing forced-failure rollback test; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security D4: `commission_apply` reads the whole AOA configuration, merges, and writes it all back; rollback restores the earlier snapshot. A scenario added in the camera UI (or by a second apply) between read and write is silently clobbered, and clobbered again on rollback. In `commission.ts`, re-read the AOA configuration immediately before `setConfiguration` and again before the rollback `setConfiguration`; if any scenario whose name is NOT managed by this spec differs from the snapshot taken in `prepare` (added, removed, or changed — compare by `JSON.stringify` per scenario keyed by name), abort that write with `AOA configuration changed underneath this run (<names>)`, record it as the write failure (or as `rollback FAILED` in the rollback case), and leave the device config untouched. Tests using the existing fake-deps harness: unmanaged scenario appears between prepare and write → no `setConfiguration` sent, verify fails with that message; same during rollback → rollback reports not verified; unchanged config → behavior identical to today — accept: `bun test` exits 0 with the new tests; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security D5 (needs `policy-check.ts`): `config_baseline` overwrites silently, so an agent holding both tools can re-baseline after a bad change and `config_drift` goes quiet. `config_baseline` refuses when `baselines/<camera>.json` already exists unless the agent's policy has `config.rebaseline: true` — deny string `baseline exists; agent '<id>' has no rebaseline grant`, receipted as `deny`. Put the decision in a pure `baselineDenied(config, exists: boolean): string | null` in `policy-check.ts`. `policy.json` is NOT edited (no agent gets the grant; Matthew re-baselines by deleting the file). Tests: no file → allowed; file + no grant → denied; file + grant → allowed — accept: `bun test` exits 0 with the new tests; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security E2: device-controlled strings reach the model verbatim inside tool output (`deviceInfo`, param values in `config_drift`/`config_remediate`, AOA error messages, event-stream error text) — a compromised or third-party camera can plant instructions there. Add `device-text.ts` exporting a pure `deviceText(value: unknown, max = 200): string` that stringifies, replaces control characters and newlines with a space, truncates to `max` with `…`, and returns the result `JSON.stringify`-quoted so it can never read as a server status line such as `DENIED:`. Apply it at those output sites only — receipts, hashes, baselines, and handoff JSON keep the raw values. Add `device-text.test.ts`: newline + `DENIED: ...` payload comes back as one quoted line; 5,000 chars truncated; non-string input handled; and one `config_drift`-shaped formatting test — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security A2 (code half): every tool reaches the camera as `root`. Allow an optional `accounts` map per camera in `cameras.json`: `{ "viewer"?: { user, credKey }, "operator"?: { user, credKey } }`. Add `account-for.ts` exporting a pure `accountFor(tool: string, cam): { user: string; credKey: string }` — `viewer` for `list_cameras`, `get_snapshot`, `config_baseline`, `config_drift`, `commission_plan`, `commission_verify`; `operator` for `ptz_move`, `ptz_preset`; the top-level `user`/`credKey` for `config_remediate` and `commission_apply`; any role missing from `accounts` falls back to the top-level account, so today's `cameras.json` behaves exactly as now. `index.ts` fetches every referenced `credKey` at startup and threads the tool name into `vapix`/`vapixPost`/`soap`/`observeEvents`. `cameras.json` is NOT edited (Matthew adds the q6325 accounts after creating them on the device). Tests: each tool maps to the expected role; missing role falls back; unknown tool falls back to top-level — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [ ] Security C2 (needs `curl-args.ts`): TLS verification is off (`-k`). Allow an optional `pin` per camera in `cameras.json` (`"sha256//<base64>"`); when present `curlRequest` emits `--pinnedpubkey <pin>` and keeps `-k` (self-signed camera certs fail chain validation but the pinned key still must match); when absent, behavior is unchanged. `cameras.json` is NOT edited. Tests: pin present → flag and value in `args`; pin absent → no `--pinnedpubkey`; a pin that does not start with `sha256//` throws — accept: `bun test` exits 0 with the new tests; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).

## Blocked / needs Matthew
- [ ] After the C1/C2 curl row merges — live smoke (Matthew, daytime): `list_cameras` + `get_snapshot` on q6358, q6325, p3285. `--digest` replaces `--anyauth`; if p3285 over HTTPS only offers Basic, that camera needs its auth policy set to digest (or basic+digest) in the web UI.
- [ ] A2 device half (Matthew, on the Q6325 only for now): create a Viewer and an Operator account on q6325. It runs `protocol: onvif`, and AXIS keeps ONVIF users separate from VAPIX users — create the pair under System → ONVIF accounts AND the matching VAPIX accounts (the snapshot fetch goes over VAPIX). Then `cred set cam-q6325-viewer` / `cred set cam-q6325-operator` and add the `accounts` block to `cameras.json` once the A2 code row has merged.
- [ ] C2 device half (Matthew): switch q6358 and q6325 `base` to `https://` in `cameras.json` (HTTPS is already on the cameras or gets enabled in the web UI), then record each pin once the C2 code row has merged: `openssl s_client -connect <ip>:443 </dev/null 2>/dev/null | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64` → `"pin": "sha256//<that>"`. The event WebSocket (`observeEvents`) still sends Basic with verification off — pinning it needs a Bun TLS check, plan separately.
- [ ] A1 real fix — NOT a nightly row (architecture): per-agent signing keys + operator-signed `policy.json` verified at load, signing key out of the agent's reach (separate OS user or Keychain). Plan with planner + `/codex-pair` before any code. Stopgap already applied 2026-09-20: `chmod 600 .keys/receipt.key`.
- [ ] E1 walk test (Matthew): printed instruction sign in frame, neutral scene question to the agent, then check the chain for tool calls nobody asked for. Fits the Jev round-2 harness.

## Done
- [x] Security D1 + A6: an agent with a config grant can move the camera clock (`Time.*` is writable), and `hardDenied` in `commission.ts` is case-sensitive. Add `"Time"` to `HARD_DENY` and make the regex `/password|user|root/i`. `policy.json` is NOT edited — `Time` stays in `config.groups` so `config_baseline`/`config_drift` still read it; only writes (`config_remediate`, `commission_*` params) are refused. Remove the `Time.NTP.Server` line from `specs/lab-baseline.yaml`. In `commission.test.ts` switch the forced-failure rollback test from `Time.NTP.Server` to `Image.I0.Appearance.Rotation`, and add two tests: a spec with `Time.NTP.Server` → `DENIED: param 'Time.NTP.Server' is hard-denied` with zero writes, and a spec with `Image.I0.password` (lowercase) → hard-denied — accept: `bun test` exits 0; `grep -rn 'Time\.' specs/` prints nothing; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [x] Security A1 stopgap: `.keys/receipt.key` was created world-readable (mode 644). Add `key-perms.ts` exporting a pure `keyModeProblem(mode: number): string | null` (non-null when any group/other bit is set, `mode & 0o077`). In `index.ts` write `receipt.key` with `{ mode: 0o600 }`, and after loading it `statSync` the file and exit non-zero printing the problem when `keyModeProblem` is non-null. Add `key-perms.test.ts`: `0o100600` → null; `0o100644`, `0o100640`, `0o100604` → message — accept: `bun test` exits 0 with the new test file; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [x] Security C1/C2 (abuse-case checklist: `2026-09-20-onvif-mcp-abuse-case-checklist.md` in the vault's `08-Agent-Output/`): camera passwords ride curl's argv (`-u user:pass`, readable via `ps`) and `--anyauth` lets a spoofed camera ask for Basic and collect the cleartext password. Add `curl-args.ts` exporting a pure `curlRequest(cam: { base, user }, password, path, opts?: { outFile?, jsonBody?, soapBody?, maxTime? })` returning `{ args: string[], stdin: string }`: `args` never contain the password, use `--digest` (never `--anyauth`), and take credentials from `--config -`; `stdin` is the curl config line `user = "<user>:<password>"` with `\` and `"` backslash-escaped. stdin now carries the config, so request bodies move to argv as `--data-raw` (bodies hold no secrets). `vapix`, `vapixPost`, and `soap` in `index.ts` build their `Bun.spawnSync` call from it; URLs, headers, `-sk`, and timeouts unchanged. Add `curl-args.test.ts`: password absent from `args` for GET, JSON POST, and SOAP; `--digest` present and `--anyauth` absent; a password containing `"` and `\` is escaped correctly; `-o <file>` kept for snapshots; JSON and SOAP bodies arrive as `--data-raw` with their content-type header — accept: `bun test` exits 0 with the new test file; `grep -n 'anyauth\|"-u"' index.ts` prints nothing; tests run offline (no camera, no `.keys/`, no writes under `receipts/`).
- [x] Add an offline test for `render-handoff.ts` using an existing `handoff/*.json` fixture: HTML output contains the scenario rows and the receipt hash — accept: `bun test` exits 0 with the new test; no `--pdf` path exercised.
- [x] `commission.test.ts` AOA-absence test: no fix needed — test already expects verify failure per 34119cc; `bun test` 13 pass 0 fail (2026-09-18; original accept hardcoded 8 pass).
